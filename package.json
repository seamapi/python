{
  "name": "@seamapi/python",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "rm -rf ./seam && node ./scripts/generate.js",
    "format": "prettier --write --ignore-path .gitignore .",
    "preformat": "make format"
  },
  "devDependencies": {
    "@seamapi/nextlove-sdk-generator": "1.8.0",
    "@seamapi/types": "1.153.2",
    "prettier": "^3.2.5"
  }
}
